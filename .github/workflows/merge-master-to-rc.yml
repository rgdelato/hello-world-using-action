name: "Nightly Merge"

# on:
#   push:
#     branches:
#       - master

on:
  workflow_dispatch:

jobs:
  nightly-merge:
    name: Merge master branch to rc branch
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Merge master to rc
        uses: robotology/gh-action-nightly-merge@v1.4.0
        with:
          stable_branch: "master"
          development_branch: "rc"
          allow_ff: true
          user_name: github-actions
          user_email: github-actions@github.com
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create pull request
        if: ${{ failure() }}
        uses: actions/github-script@v6
        with:
          script: |
            const test = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "⚠️ Merge master branch to rc branch ⚠️",
              head: "master",
              base: "rc",
              body: "Auto-merging master branch into rc branch failed due to conflicts. Please perform this merge manually."
            })
            console.log(test)
